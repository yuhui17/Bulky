@model List<BulkyBook.Models.Product>

<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">
                Product List
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-square"></i>
                Create New Product
            </a>
        </div>
    </div>


    <table id="tblData" class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>Title</th>
                <th>ISBN</th>
                <th>Price</th>
                <th>Author</th>
                <th>Category</th>
                <th></th> @* this head must be added otherwise the dataTable wont working *@
            </tr>
        </thead>
@*         <tbody>
 *@@*             @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    <td>@item.ISBN</td>
                    <td>@item.ListPrice</td>
                    <td>@item.Author</td>
                    <td>@item.Category.Name</td>

                    <td>
                        <div class="w-75 btn-group" role="group"> *@
                            @* adding asp-route-??? will automatically pass the value to the parameter[note: paramater must same with ???]*@
@*                             <a asp-controller="Product" asp-action="Upsert" asp-route-productId="@item.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i>
                                Edit
                            </a>
                            <a asp-controller="Product" asp-action="Delete" asp-route-productId="@item.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash3"></i>
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
            } *@
@*         </tbody>
 *@
    </table>
</div>

@section Scripts{
    <script src="~/js/product.js"></script>
}